/** @type {import("@sveltejs/vite-plugin-svelte").SvelteConfig} */
export default {
  // Explicit config so vite-plugin-svelte doesn't fall back to defaults
  // with a "no Svelte config found" warning on every `dev:ui` start.
};
